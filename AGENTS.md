# Repository Guidance

## Start here

1. Read `docs/WEATHER_SCENARIO_MATRIX.md` for active M11 identity/coverage/deltas/Worker/export contracts and evidence, `docs/MULTIFLOOR_PARAMETRIC_EXPLORER.md` / `docs/PARAMETRIC_EXPLORER.md` for completed M10/M9 contracts, `docs/VERCEL_OPERATION.md` and `docs/RELEASE_GATE.md` for operation/provenance gates, `docs/USER_GUIDE.md` for the in-app manual, and `docs/VALIDATION_PLAN.md` §10 for M5 local validation/external boundaries. Resolve live HEAD from Git/PR separately from recorded checkpoints.
2. M4.5 is terminal: `.agent-run/LR-20260914-FSL-M45-001/RUN_STATE.md` is historical audit context, not an active campaign. Verify its Task Packet SHA-256 when auditing it; do not create another M4.5 closeout cycle.
3. Check the current branch, HEAD, remote, and working tree before editing.
4. Preserve unrelated or pre-existing changes.

## Architecture

- UI: Vite + React + TypeScript in `src/app/`.
- Calculation engine: Pure TypeScript in `src/engine/`.
- Domain types: `src/models/`.
- Weather ingestion and normalization: `src/weather/`.
- Facade geometry primitives: Pure TypeScript in `src/geometry/`.
- Comparison cases, deltas, and input differences: Pure TypeScript in `src/comparison/`.
- Multi-floor composition, aggregation, presets, and exports: Pure TypeScript in `src/multifloor/`.
- Weather scenario identity, compatibility, canonical composition, snapshots and exports: Pure TypeScript in `src/scenario/`.
- Never import React, DOM, Canvas, or browser globals into the calculation engine.
- Keep core calculations deterministic and usable from Browser, Node.js, batch tests, and validation scripts.
- Keep the original-source reference generator independent from `src/engine/`; expected Golden values must never be generated by the new engine.
- Keep `legacy-v01` and `weather-v1` separate; neither engine may silently call or rewrite the other.

## Domain constraints

- Declare units at module boundaries. Prefer SI internally; never mix mm and m implicitly.
- Define angle convention, facade azimuth convention, time zone, and coordinate system explicitly.
- Use tolerances for geometry and floating-point comparisons.
- Shading geometry with a window-head offset depends on both `D/H` and `O/H`.
- Do not present unvalidated absolute `[kWh]` results as formal performance evidence.
- Keep weather-source provenance visible; do not disguise empirical factors as physical models.
- Treat M1 absolute `[kWh]` values only as `LEGACY BASELINE / NOT VALIDATED PHYSICAL MODEL` regression data.
- EPW uses Local Standard Time and interval-end radiation in `Wh/m2 interval`; evaluate weather-v1 geometry at the interval midpoint without `Date`, DST, or host-timezone dependence.
- Reject missing/invalid required solar data by default; never silently zero-fill it.
- Facade-local coordinates use wall `y = 0`, front-view `+x` right, outward `+y`, and `+z` up; azimuth is north-zero/clockwise.
- Keep M3 finite-width geometry limited to direct shadow; its diffuse model remains the explicitly identified M2-compatible 2D infinite-width approximation.

## Required checks

```bash
npm test
npm run typecheck
npm run build
npm run golden:check
npm audit
```

Add focused tests for changed engine behavior. Do not fabricate validation results.

## Scope and gates

- Current milestone: M11 — Climate / Weather Scenario Matrix: IMPLEMENTATION_COMPLETE / HUMAN_UX_REVIEW_PENDING / INDEPENDENT_FULL_REVIEW_PENDING. See `docs/WEATHER_SCENARIO_MATRIX.md` for evidence and explicitly unverified browser/saved-file items. At most 4 weather × 4 current workspace designs; no parametric Cartesian sweep/ranking. Preserve exact canonical Single/Multi routes, content identity, year-independent temporal compatibility, separate Design/Weather deltas, NOT_COMPARABLE/INVALID without zero substitution, immutable snapshots/sticky STALE, terminable Worker, unresolved input-only JSON. M10 COMPLETE: PR #15 merged, main `56a0f19eb4103c5231f09ec1c79dcfe2f782d783`, automatic Production `dpl_FLzFM4fpWcXr2NSZmCe71ez4dwY3` Git/main/exact SHA/READY; canonical HTTP/assets/both Explorer Workers PASS. Historical M10 Human/Agent attribution and non-blocking PDF Advisory remain unchanged. M9/M8/M7/M6 COMPLETE. M5 remains LOCAL_VALIDATION_COMPLETE / EXTERNAL_REFERENCE_PENDING; Radiance / EnergyPlus / SPA / annual physical comparison NOT_RUN. No formal absolute-kWh validation. Preserve P0-B checkpoint/protocol. Ready/merge/manual Production require separate Human authorization. M12 NOT STARTED.
- M7 provides opening-local horizontal overhang / jamb fins / intermediate vertical fin arrays, direct shadows only. Arrays use centred pitch/count derivation, maximum 128, explicit errors (no truncation), and deterministic analytic x-sweep union. Keep the pre-array `2ccbcc89cfe475df4df5a5ec42f0c899b250deaa` edge-fin checkpoint. Preserve facade-v1-weather equivalence without fins, shared solar/weather/energy aggregation, and the existing approximate diffuse/ground models. Cross-floor physical shading remains unimplemented. No formal absolute physical validation is claimed.
- M8 is static client documentation and hash navigation only. Preserve mounted Single/Multi workspace state across `#guide` transitions. Do not add a router, analytics, external iframe/font/image, network upload, raw EPW persistence, local path persistence, or calculation-physics changes for Guide content. Model IDs, periods, and fin limits should stay tied to exported contracts and covered by tests.
- Normal feature push may trigger Git Preview. Git main merge may trigger automatic Production only after separate Human merge authorization. Manual Production deploy/promote/rollback, Vercel settings/env/protection changes and bypass-token generation remain unauthorized. Never use `vercel curl` for verification.
- Do not mutate `main`, mark a PR Ready, merge, deploy Production, release, force-push, delete branches, or change repository permissions/visibility without a new Human instruction.
- Never commit secrets, credentials, private URLs, personal data, client/project identities, unpublished design data, or user-specific absolute local paths.
- Legacy MVP sources must remain byte-for-byte unchanged and retain the SHA-256 values recorded in `docs/LEGACY_BASELINE.md`.
- Do not commit external raw weather files unless redistribution rights are explicitly established; retain public-safe provenance and hashes.
- Preserve `src/weather/**` and `src/engine/weather-v1/**` as the M2 baseline unless a separate authorized fix requires otherwise.
